#ifndef DESKTOP_SHELL_H
#define DESKTOP_SHELL_H

#include <stdint.h>
#include <stdbool.h>

// Initialize the desktop shell
void desktop_shell_init(void);

// Start the desktop shell main loop
void desktop_shell_start(void);

#endif // DESKTOP_SHELL_H
